# =============================================================================
# BIONIC AGENTS - ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# This file contains all environment variables needed across the three
# applications: Agent-Builder, Agent-Dashboard, and Agent-Runtime.
#
# Copy this file to .env and update with your actual values.
# =============================================================================

# =============================================================================
# SHARED CONFIGURATION (Used by all applications)
# =============================================================================

# Database Configuration (PostgreSQL)
# All three applications share the same database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/liveagents

# Application Environment
NODE_ENV=development

# =============================================================================
# OAUTH AUTHENTICATION (Used by Agent-Builder and Agent-Dashboard)
# =============================================================================

# OAuth Application ID (required for authentication)
VITE_APP_ID=your-oauth-app-id

# OAuth Server Backend URL (for token exchange)
OAUTH_SERVER_URL=http://localhost:3000

# OAuth Portal URL (for frontend login redirect)
VITE_OAUTH_PORTAL_URL=http://localhost:3000

# JWT Secret for session cookie signing (min 32 characters)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-in-production

# Owner OpenID (optional - for admin access)
OWNER_OPEN_ID=

# =============================================================================
# AGENT-BUILDER CONFIGURATION
# =============================================================================

# Application Port
PORT=3000

# Application Title (displayed in UI)
VITE_APP_TITLE=LiveKit Agent Builder

# Application Logo URL (optional)
VITE_APP_LOGO=/favicon.ico

# Forge API Configuration (for map features - optional)
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# Agent Runtime API Configuration (for deploying agents)
AGENT_RUNTIME_API_URL=http://localhost:8080
AGENT_RUNTIME_API_KEY=your-runtime-api-key

# Analytics Configuration (optional)
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# =============================================================================
# AGENT-DASHBOARD CONFIGURATION
# =============================================================================

# Application Port
PORT=3001

# Application Title (displayed in UI)
VITE_APP_TITLE=LiveKit Dashboard

# Application Logo URL (optional)
VITE_APP_LOGO=/favicon.ico

# API URL (for frontend API calls)
VITE_API_URL=http://localhost:3001

# Static Directory (for production builds)
STATIC_DIR=/app/dist/public

# Forge API Configuration (for map features - optional)
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# =============================================================================
# AGENT-RUNTIME CONFIGURATION
# =============================================================================

# Application Port
PORT=8080

# Runtime API Key (for authenticating API requests from Agent-Builder)
AGENT_RUNTIME_API_KEY=your-runtime-api-key-min-32-chars

# Runtime Limits
MAX_AGENTS_PER_INSTANCE=50
MAX_SESSIONS_PER_AGENT=100

# =============================================================================
# LIVEKIT CONFIGURATION (Used by all applications)
# =============================================================================

# LiveKit Server WebSocket URL
LIVEKIT_URL=wss://livekit.bionicaisolutions.com

# LiveKit API Key
LIVEKIT_API_KEY=your-livekit-api-key

# LiveKit API Secret
LIVEKIT_API_SECRET=your-livekit-api-secret-min-32-chars

# =============================================================================
# LANGFUSE CONFIGURATION (Optional - Used by Agent-Builder and Agent-Runtime)
# =============================================================================


# Enable LangFuse tracing (true/false)
LANGFUSE_ENABLED=true

# LangFuse Public API Key
LANGFUSE_PUBLIC_KEY=pk-lf-8ab6897f-35ec-4914-9e6a-636f02d46aeb
# LangFuse Secret API Key
LANGFUSE_SECRET_KEY=sk-lf-337fe33b-e45f-42da-9687-df132a28a3d8

# LangFuse Base URL (default: https://cloud.langfuse.com)
LANGFUSE_BASE_URL=https://langfuse.bionicaisolutions.com

# =============================================================================
# NOTES
# =============================================================================
#
# 1. DATABASE_URL: All three applications share the same PostgreSQL database.
#    Make sure the database exists and migrations have been run.
#
# 2. OAuth Configuration: Required for Agent-Builder and Agent-Dashboard.
#    If you don't have an OAuth server, you can use placeholder values,
#    but authentication will not work.
#
# 3. LiveKit Configuration: Required for all applications to connect to
#    your LiveKit server for real-time communication.
#
# 4. LangFuse: Optional tracing and observability. Set LANGFUSE_ENABLED=false
#    if you don't want to use it.
#
# 5. Port Configuration: Each application uses a different port by default:
#    - Agent-Builder: 3000
#    - Agent-Dashboard: 3001
#    - Agent-Runtime: 8080
#
# 6. JWT_SECRET and AGENT_RUNTIME_API_KEY: Use strong, random values in
#    production (minimum 32 characters).
#
# =============================================================================
