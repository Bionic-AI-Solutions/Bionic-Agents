apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
  namespace: livekit
  labels:
    app: agent-runtime
spec:
  selector:
    app: agent-runtime
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP

